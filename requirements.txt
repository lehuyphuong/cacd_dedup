# cacd-dedup dependencies
# Python 3.10+

# Dataset
datasets>=2.19.0
huggingface-hub>=0.23.0

# Chunking
langchain-text-splitters>=0.2.0

# Embedding (Stage 0)
sentence-transformers>=3.0.0
torch>=2.1.0

# Cross-attention dedup (Stage 2) — cross-encoder/ms-marco-MiniLM-L-6-v2
transformers>=4.40.0

# Vector store (embedded, no Docker) — also serves as the persistent
# index that Stage 1 (coarse retrieval) queries against
qdrant-client>=1.9.0

# NLP — spaCy required for preprocessed eval tokenization mode
spacy>=3.7.0
# After install: python -m spacy download en_core_web_sm

# TopicBased chunker — k-means clustering on sentence embeddings
scikit-learn>=1.4.0

# Heatmap visualization (Stage 2 attention matrix → results/heatmaps/)
matplotlib>=3.8.0

# Numerics
numpy>=1.26.0

# HTTP (Ollama — optional, not used in current scope)
httpx>=0.27.0
